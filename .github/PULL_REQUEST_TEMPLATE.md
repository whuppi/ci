<!-- What does this PR do and why? -->

**Consumer impact:** <!-- None / additive (MINOR) / caller stubs or Makefile contract change (MAJOR). Which consumers must adapt, and how? -->

**Verified:** <!-- `make check` output, and for behavior changes: how you proved parity or the new behavior (a consumer dry-run, a diff against the old output...). -->

<!-- Before submitting: `make check` must pass locally. PRs target `main`. Internal refs stay `@main` — self-release stamps them. See CONTRIBUTING.md. -->
